const Header = () => {
  return (
    <header>
      <div>
        <h2>GAMEPAD</h2>
      </div>

      <nav>
        <button>My Collection</button>
        <button className="login-button">Login</button>
      </nav>
    </header>
  );
};

export default Header;
